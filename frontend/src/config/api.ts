/**
 * API 配置文件
 * 
 * 使用相对路径，通过 Vite 代理连接到后端
 * 这样可以避免跨域问题和主机地址问题
 */

export const API_CONFIG = {
  /**
   * 后端 API 基础地址
   * 
   * 使用空字符串表示相对路径，Vite 开发服务器会自动代理到后端
   * 生产环境构建时，可以设置为实际的后端服务器地址
   * 
   * 开发环境：使用相对路径（通过 Vite 代理）
   * 生产环境：可以设置为实际的后端地址，例如：
   * - '' (空字符串，使用相对路径)
   * - 'http://192.168.12.132:8002'
   * - 'https://api.your-domain.com'
   */
  BASE_URL: '',
};

export default API_CONFIG;
